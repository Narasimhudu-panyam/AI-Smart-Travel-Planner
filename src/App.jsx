import React, { useEffect, useMemo, useState } from "react";
import { BrowserRouter, Navigate, NavLink, Outlet, Route, Routes, useLocation, useNavigate, useParams } from "react-router-dom";
import { ArrowRight, CalendarDays, Check, ChevronDown, CloudSun, Download, FileUp, Heart, History, Loader2, LogOut, Map, Menu, Moon, Plane, Plus, Share2, Sparkles, Sun, UserRound, Wallet } from "lucide-react";
import { fetchTrips, generateTrip, uploadDocument } from "./api";
import { firebaseEnabled, loginWithEmail, loginWithGoogle, logout, registerWithEmail, subscribeToAuth } from "./firebase";
import { interestOptions, styleOptions } from "./data";
import PopularPlaces from "./PopularPlaces";
import FavoriteDestinations from "./FavoriteDestinations";

const today = new Date().toISOString().slice(0, 10);
const initialForm = { destination: "Goa, India", start_date: today, end_date: today, budget: 750, currency: "INR", travelers: 2, interests: ["Food", "Beaches", "Photography"], travel_style: "balanced", notes: "", selected_attractions: [] };
const formatCurrency = (value, currency = "INR") => new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 }).format((currency === "INR" ? value : Number(value || 0) * 83) || 0);

function App() {
  const [user, setUser] = useState(null);
  const [dark, setDark] = useState(false);
  useEffect(() => subscribeToAuth(setUser), []);
  useEffect(() => { document.documentElement.dataset.theme = dark ? "dark" : "light"; }, [dark]);
  return <BrowserRouter><Shell user={user} setUser={setUser} dark={dark} setDark={setDark} /></BrowserRouter>;
}

function Shell({ user, setUser, dark, setDark }) {
  const navigate = useNavigate();
  const location = useLocation();
  async function signOut() { await logout(); setUser(null); navigate("/"); }
  const plain = ["/login", "/register"].includes(location.pathname);
  return <><Nav user={user} onLogout={signOut} dark={dark} setDark={setDark} plain={plain} />
    <Routes>
      <Route path="/" element={<Home user={user} />} />
      <Route path="/login" element={user ? <Navigate to="/dashboard" replace /> : <Login onLogin={setUser} />} />
      <Route path="/register" element={user ? <Navigate to="/dashboard" replace /> : <Register onLogin={setUser} />} />
      <Route element={<Protected user={user} />}>
        <Route path="/dashboard" element={<Dashboard user={user} />} />
        <Route path="/create-trip" element={<CreateTrip user={user} />} />
        <Route path="/trip/:id" element={<TripDetails user={user} />} />
        <Route path="/history" element={<HistoryPage user={user} />} />
        <Route path="/profile" element={<Profile user={user} onLogout={signOut} />} />
        <Route path="/settings" element={<Profile user={user} onLogout={signOut} settings />} />
      </Route>
      <Route path="*" element={<Navigate to="/" replace />} />
    </Routes></>;
}
function Protected({ user }) { return user ? <Outlet /> : <Navigate to="/login" replace />; }
function Nav({ user, onLogout, dark, setDark, plain }) { const [open, setOpen] = useState(false); return <header className={`nav ${plain ? "nav-plain" : ""}`}><NavLink className="logo" to="/"><span><Plane size={19}/></span>TravelFlow</NavLink><button className="mobile-menu" onClick={() => setOpen(!open)}><Menu/></button><nav className={open ? "open" : ""}>{!user ? <><a href="/#features">Features</a><a href="/#about">About</a><a href="/#contact">Contact</a></> : <><NavLink to="/dashboard">Dashboard</NavLink><NavLink to="/history">My Trips</NavLink></>}<button className="theme" onClick={() => setDark(!dark)} aria-label="Toggle dark mode">{dark ? <Sun size={18}/> : <Moon size={18}/>}</button>{user ? <><NavLink className="profile-link" to="/profile"><UserRound size={17}/>{user.displayName?.split(" ")[0] || "Profile"}</NavLink><button className="logout-link" onClick={onLogout}>Logout</button></> : <><NavLink className="login-link" to="/login">Login</NavLink><NavLink className="button small" to="/register">Get started</NavLink></>}</nav></header>}

function Home({ user }) { return <main><section className="hero"><div className="hero-content"><p className="eyebrow">YOUR JOURNEY, INTELLIGENTLY PLANNED</p><h1>Travel more. <em>Plan less.</em></h1><p className="hero-copy">AI-crafted itineraries, perfectly balanced budgets, and every remarkable moment in one beautiful place.</p><div className="hero-actions"><NavLink className="button" to={user ? "/create-trip" : "/register"}>Plan your trip <ArrowRight size={18}/></NavLink><a className="text-link" href="#how">See how it works</a></div><div className="hero-proof"><div className="avatars"><i>J</i><i>M</i><i>A</i><i>S</i></div><span>Loved by 12,000+ happy travelers</span></div></div><div className="hero-card"><div className="trip-chip"><span>✦</span> Your next escape</div><div className="hero-destination"><p>EXPLORE</p><h2>Amalfi Coast</h2><span>Italy · 7 days · From $1,850</span></div><div className="mini-plan"><div><CalendarDays size={18}/><span><b>Day 03</b> · Ravello & Positano</span></div><div className="mini-dots"><b></b><b></b><b></b></div></div></div></section><section className="logos"><span>Trusted by travelers from</span><b>NomadList</b><b>✦ WANDER</b><b>roam</b><b>Voyage</b></section><section id="features" className="section"><p className="eyebrow">SMARTER TRAVEL STARTS HERE</p><h2>Everything you need,<br/><em>nothing you don’t.</em></h2><div className="feature-grid"><Feature icon={<Sparkles/>} title="AI-powered itineraries" text="Thoughtful, personalized days built around the way you love to travel."/><Feature icon={<Wallet/>} title="Budgets that work" text="See exactly where your money goes before you book a thing."/><Feature icon={<Map/>} title="All in one place" text="Maps, plans, bookings, and notes — beautifully organized."/></div></section><section id="how" className="how section"><div><p className="eyebrow">SIMPLE BY DESIGN</p><h2>From daydream<br/>to departure.</h2></div><ol><li><b>01</b><div><h3>Tell us where</h3><p>Share your destination, dates, and what lights you up.</p></div></li><li><b>02</b><div><h3>Let AI work its magic</h3><p>Get a personalized itinerary in seconds, not hours.</p></div></li><li><b>03</b><div><h3>Make it yours</h3><p>Tweak, save, and head out with confidence.</p></div></li></ol></section><section className="destinations section"><p className="eyebrow">GET INSPIRED</p><h2>Where will you go next?</h2><div className="destination-grid">{[["Kyoto","Japan","kyoto"],["Santorini","Greece","santorini"],["Bali","Indonesia","bali"],["Lisbon","Portugal","lisbon"]].map(([name, country, cls]) => <NavLink key={name} to={user ? "/create-trip" : "/register"} className={`destination ${cls}`}><span>{country}</span><h3>{name}</h3><ArrowRight size={18}/></NavLink>)}</div></section><section className="quote section"><p>“Finally, a travel planner that feels like it gets me.”</p><span>— Maya R., frequent wanderer</span></section><section id="about" className="faq section"><p className="eyebrow">GOOD TO KNOW</p><h2>Questions, answered.</h2>{["Is TravelFlow free to use?","How does the AI create my itinerary?","Can I edit my itinerary after it’s created?"].map(q => <details key={q}><summary>{q}<ChevronDown size={18}/></summary><p>Absolutely. Start planning for free, then personalize every detail to make each journey truly yours.</p></details>)}</section><footer id="contact"><div className="logo"><span><Plane size={18}/></span>TravelFlow</div><p>Plan less. Experience more.</p><span>© 2026 TravelFlow. Made for the curious.</span></footer></main> }
function Feature({ icon, title, text }) { return <article className="feature"><span className="feature-icon">{icon}</span><h3>{title}</h3><p>{text}</p><a href="#how">Learn more <ArrowRight size={15}/></a></article> }

function AuthLayout({ title, subtitle, children }) { return <main className="auth-page"><div className="auth-art"><p>TRAVELFLOW</p><h1>Every journey begins<br/>with a little <em>wonder.</em></h1><span>✦</span></div><section className="auth-card"><p className="eyebrow">WELCOME TO TRAVELFLOW</p><h1>{title}</h1><p>{subtitle}</p>{children}</section></main> }
function Login({ onLogin }) { const navigate = useNavigate(); const [data, setData] = useState({ email: "", password: "" }); const [error, setError] = useState(""); const [busy, setBusy] = useState(false); const submit = async e => {e.preventDefault();setBusy(true);setError("");try {onLogin(await loginWithEmail(data.email, data.password));navigate("/dashboard");} catch(err) {setError(err.message)} finally {setBusy(false)}}; const google = async()=> {try {onLogin(await loginWithGoogle());navigate("/dashboard")}catch(err){setError(err.message)}}; return <AuthLayout title="Welcome back." subtitle="Your next adventure is waiting."><form className="auth-form" onSubmit={submit}><label>Email address<input required type="email" placeholder="you@example.com" onChange={e=>setData({...data,email:e.target.value})}/></label><label>Password<input required type="password" placeholder="••••••••" onChange={e=>setData({...data,password:e.target.value})}/></label><div className="form-row"><label className="check"><input type="checkbox"/>Remember me</label><a href="#forgot">Forgot password?</a></div>{error&&<p className="error">{error}</p>}<button className="button" disabled={busy}>{busy&&<Loader2 className="spin"/>}Log in <ArrowRight size={17}/></button></form><div className="or">or continue with</div><button className="google" onClick={google}>G <span>Google</span></button><p className="auth-switch">New here? <NavLink to="/register">Create an account</NavLink></p>{!firebaseEnabled&&<p className="demo-note">Demo mode: register an account to sign in.</p>}</AuthLayout> }
function Register({ onLogin }) { const navigate=useNavigate(); const [data,setData]=useState({name:"",email:"",password:"",confirm:""});const [error,setError]=useState("");const submit=async e=>{e.preventDefault();if(data.password.length<8)return setError("Use at least 8 characters for your password.");if(data.password!==data.confirm)return setError("Passwords do not match.");try{onLogin(await registerWithEmail(data.name,data.email,data.password));navigate("/dashboard")}catch(err){setError(err.message)}};return <AuthLayout title="Begin your story." subtitle="Create your account and start exploring."><form className="auth-form" onSubmit={submit}><label>Full name<input required placeholder="Alex Morgan" onChange={e=>setData({...data,name:e.target.value})}/></label><label>Email address<input required type="email" placeholder="you@example.com" onChange={e=>setData({...data,email:e.target.value})}/></label><label>Password<input required minLength="8" type="password" placeholder="8+ characters" onChange={e=>setData({...data,password:e.target.value})}/></label><label>Confirm password<input required type="password" placeholder="Repeat your password" onChange={e=>setData({...data,confirm:e.target.value})}/></label>{error&&<p className="error">{error}</p>}<button className="button">Create account <ArrowRight size={17}/></button></form><p className="auth-switch">Already a member? <NavLink to="/login">Log in</NavLink></p></AuthLayout> }

function Dashboard({ user }) { const [trips,setTrips]=useState([]);useEffect(()=>{fetchTrips(user.uid).then(setTrips).catch(()=>setTrips([]))},[user.uid]); const total=trips.length;return <main className="app-page"><div className="page-heading"><div><p className="eyebrow">YOUR TRAVEL SPACE</p><h1>Welcome back, {user.displayName?.split(" ")[0] || "Traveler"}.</h1><p>Ready to turn your next daydream into a real journey?</p></div><NavLink className="button" to="/create-trip"><Plus size={18}/> Plan new trip</NavLink></div><div className="stats-cards"><Stat icon={<Plane/>} label="Total trips" value={total}/><Stat icon={<CalendarDays/>} label="Upcoming trips" value={total}/><Stat icon={<Heart/>} label="Saved places" value="24"/><Stat icon={<Wallet/>} label="Total budget" value={formatCurrency(trips.reduce((a,t)=>a+(t.plan?.budget||0),0) || 2450)}/></div><section className="dashboard-grid"><article className="dashboard-card"><div className="card-title"><h2>Recent trips</h2><NavLink to="/history">View all</NavLink></div>{trips.length ? trips.slice(0,3).map(t=><TripRow key={t.id} trip={t}/>) : <EmptyTrips/>}</article><aside className="dashboard-card actions"><h2>Quick actions</h2><NavLink to="/create-trip"><Sparkles/>Generate an itinerary <ArrowRight size={16}/></NavLink><NavLink to="/history"><History/>Trip history <ArrowRight size={16}/></NavLink><NavLink to="/profile"><UserRound/>Profile & settings <ArrowRight size={16}/></NavLink></aside></section></main>}
function Stat({icon,label,value}){return <article className="stat-card"><span>{icon}</span><p>{label}</p><strong>{value}</strong></article>} function EmptyTrips(){return <div className="empty-trips"><Plane/><h3>Your adventure starts here.</h3><p>Create your first AI itinerary and keep every detail in one place.</p><NavLink className="text-link" to="/create-trip">Plan a trip <ArrowRight size={16}/></NavLink></div>}function TripRow({trip}){return <NavLink className="trip-row" to={`/trip/${trip.id}`}><span className="trip-thumb"><Plane/></span><div><b>{trip.plan.destination}</b><p>{trip.plan.summary}</p></div><ArrowRight size={17}/></NavLink>}

function CreateTrip({user}){const navigate=useNavigate();const [form,setForm]=useState(initialForm);const[loading,setLoading]=useState(false);const[error,setError]=useState("");const days=useMemo(()=>Math.max(1,Math.round((new Date(form.end_date)-new Date(form.start_date))/86400000)+1),[form]);const update=(key,value)=>setForm(f=>({...f,[key]:["budget","travelers"].includes(key)?Number(value):value}));const toggle=i=>setForm(f=>({...f,interests:f.interests.includes(i)?f.interests.filter(x=>x!==i):[...f.interests,i]}));const submit=async e=>{e.preventDefault();setLoading(true);setError("");try{const plan=await generateTrip({...form,user_id:user.uid});const cached=JSON.parse(localStorage.getItem("latest-plan")||"{}");localStorage.setItem("latest-plan",JSON.stringify(plan));navigate(`/trip/${plan.id || cached.id || "latest"}`,{state:{plan}})}catch(err){setError(err.message)}finally{setLoading(false)}};return <main className="app-page"><div className="page-heading compact-page"><div><p className="eyebrow">BUILD YOUR ESCAPE</p><h1>Plan a new trip.</h1><p>Tell us the essentials. We’ll handle the delightful details.</p></div></div><form className="trip-builder" onSubmit={submit}><section className="form-card"><label>Where would you like to go?<input value={form.destination} onChange={e=>update("destination",e.target.value)}/></label><div className="form-grid"><label>Departure<input type="date" value={form.start_date} onChange={e=>update("start_date",e.target.value)}/></label><label>Return<input type="date" value={form.end_date} onChange={e=>update("end_date",e.target.value)}/></label></div><div className="form-grid"><label>Budget<input min="1" type="number" value={form.budget} onChange={e=>update("budget",e.target.value)}/></label><label>Travelers<input min="1" type="number" value={form.travelers} onChange={e=>update("travelers",e.target.value)}/></label></div><label>Travel style</label><div className="choice-row">{styleOptions.map(s=><button type="button" key={s.value} className={form.travel_style===s.value?"selected":""} onClick={()=>update("travel_style",s.value)}>{s.label}</button>)}</div><label>What are you into?</label><div className="choice-row">{interestOptions.map(i=><button type="button" key={i} className={form.interests.includes(i)?"selected":""} onClick={()=>toggle(i)}>{form.interests.includes(i)&&<Check size={14}/>} {i}</button>)}</div><PopularPlaces destination={form.destination} selected={form.selected_attractions} onChange={v=>update("selected_attractions",v)}/>{error&&<p className="error">{error}</p>}<button className="button generate" disabled={loading}>{loading?<Loader2 className="spin"/>:<Sparkles/>}{loading?"Creating your itinerary...":`Generate ${days}-day AI itinerary`}<ArrowRight size={17}/></button></section><aside className="builder-aside"><span>✦</span><h2>The world is<br/>waiting for you.</h2><p>Every itinerary is built around your style, interests, and pace.</p><div><b>{days} days</b><b>{form.travelers} travelers</b><b>{form.travel_style}</b></div></aside></form></main>}

function TripDetails({user}){const {id}=useParams();const location=useLocation();const [plan,setPlan]=useState(location.state?.plan||null);const[upload,setUpload]=useState("");useEffect(()=>{if(plan)return;fetchTrips(user.uid).then(items=>setPlan(items.find(x=>String(x.id)===id)?.plan||JSON.parse(localStorage.getItem("latest-plan")||"null"))).catch(()=>setPlan(JSON.parse(localStorage.getItem("latest-plan")||"null")))},[id,user.uid,plan]);if(!plan)return <main className="app-page"><EmptyTrips/></main>;const map=`https://maps.google.com/maps?q=${encodeURIComponent(plan.map_query||plan.destination)}&output=embed`;return <main className="app-page"><div className="trip-summary"><div><p className="eyebrow">YOUR AI-CRAFTED JOURNEY</p><h1>{plan.destination}</h1><p>{plan.summary}</p></div><div className="trip-actions"><button onClick={()=>window.print()}><Download size={17}/> Download PDF</button><button><Share2 size={17}/> Share</button></div></div><div className="trip-meta"><span><CalendarDays/> {plan.start_date || "Your dates"}</span><span><CloudSun/> {plan.weather?.temperature_c ? `${plan.weather.temperature_c}°C` : "Weather ready"}</span><span><Wallet/> {formatCurrency(plan.budget,plan.currency)}</span></div><div className="itinerary-layout"><section><div className="map-box"><iframe title="Trip location" src={map}/></div><h2>Day-by-day plan</h2>{plan.itinerary?.map(day=><article className="day-plan" key={day.day}><div className="day-number">DAY {day.day}<span>{day.date}</span></div><div>{day.activities.map(a=><div className="activity" key={a.time+a.place}><time>{a.time}</time><div><h3>{a.place}</h3><p>{a.description}</p><span>{a.duration} {a.estimated_cost ? `· ${formatCurrency(a.estimated_cost,plan.currency)}`:""}</span></div></div>)}</div></article>)}</section><aside className="details-side"><section><h3>Budget overview</h3>{Object.entries(plan.budget_breakdown||{}).map(([k,v])=><p key={k}><span>{k}</span><b>{formatCurrency(v,plan.currency)}</b></p>)}</section><section><h3>Packing list</h3>{plan.packing_tips?.map(x=><p className="packing" key={x}><Check size={15}/>{x}</p>)}</section><section><h3>Travel documents</h3><label className="upload"><input type="file" onChange={async e=>{if(e.target.files?.[0])setUpload((await uploadDocument(e.target.files[0])).url)}}/><FileUp/> Upload booking or ID</label>{upload&&<a href={upload}>Document uploaded</a>}</section><section><h3>Local tips</h3>{plan.local_tips?.map(x=><p className="packing" key={x}>✦ {x}</p>)}</section></aside></div></main>}
function HistoryPage({user}){const[trips,setTrips]=useState([]);useEffect(()=>{fetchTrips(user.uid).then(setTrips).catch(()=>setTrips([]))},[user.uid]);return <main className="app-page"><div className="page-heading"><div><p className="eyebrow">YOUR MEMORIES</p><h1>Trip history.</h1><p>All the journeys you’ve planned, in one place.</p></div><NavLink className="button" to="/create-trip"><Plus size={18}/> Plan a trip</NavLink></div><section className="history-page">{trips.length?trips.map(t=><TripRow key={t.id} trip={t}/>):<EmptyTrips/>}</section></main>}
function Profile({user,onLogout,settings}){return <main className="app-page profile-page"><p className="eyebrow">{settings?"PREFERENCES":"YOUR PROFILE"}</p><h1>{settings?"Settings":"Traveler profile."}</h1><section className="profile-card"><div className="profile-avatar">{user.displayName?.[0]||"T"}</div><div><h2>{user.displayName||"Traveler"}</h2><p>{user.email}</p></div><button className="outline">Edit profile</button></section><section className="settings-list"><FavoriteDestinations firebaseUid={user.uid}/><div><h3>Email notifications</h3><button className="toggle"><span/></button></div><div><h3>Appearance</h3><p>Use the sun/moon switch in the navigation to change your theme.</p></div></section><button className="danger" onClick={onLogout}><LogOut size={17}/> Log out</button></main>}
export default App;
